# Chrome proxy setup

This extension allows the user to change Chrome proxy configuration for incognito sessions which does not affect normal browser sessions.

1. Clone the repo: `git clone https://github.com/vulnbe/chrome-proxy-session`
1. Еnable developer mode in your browser [settings](chrome://extensions/)
1. Load the unpacked extension and allow it to be used in incognito mode
